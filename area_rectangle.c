//program to find area of recatngle
#include <stdio.h>
int main()
{
  int length,breadth,area;
printf("enter length,breadth\n");
scanf("%d %d ",&length,&breadth);
area=length*breadth;
printf("area of rectangle=%d",&area);
return 0;
}
